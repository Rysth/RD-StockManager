Hola <%= @email %>,

Recibimos una solicitud para restablecer tu contraseña.

Restablece tu contraseña en el siguiente enlace:
<%= FrontendUrls.admin_url %>/auth/reset-password/<%= @token %>

Si no solicitaste este cambio, ignora este correo.